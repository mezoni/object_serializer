name: object_serializer
description: A serializer (with caching support) of any static (generic) complex data that can be represented as simpler data.
repository:  https://github.com/mezoni/object_serializer
version: 0.3.7
environment:
  sdk: '>=2.17.0 <3.0.0'
dependencies:
  build: any
  code_builder: any
  dart_style: any
  yaml: ^3.1.1
dev_dependencies:
  build_runner: any
  http: any
  lints: ^2.0.0
  test: ^1.21.4
  tuple: ^2.0.0

